{% extends 'layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
          <h3 class="page-header"><i class="icon_house_alt" aria-hidden="true"></i> Editar unidad de carga </h3>
            <a class="btn btn-primary" href="{{ path('unidadcarga_index') }}" title="Volver"><i class="fa fa fa-reply-all" aria-hidden="true"></i>  Volver</a> 
        </div>
    </div>
    <br>
   {{ form_start(edit_form, { 'attr' : {'role' : 'form'}}) }}
	    <div class="row">	
			<div class="col-lg-12">
				<section class="panel">
				    <header class="panel-heading">
						Unidad de Carga
					</header>
					<div class="panel-body">
						<div class="form-group col-lg-4">
							{{ form_label(edit_form.nombre) }}
							{{ form_widget(edit_form.nombre) }}
						</div>
	
					<div class="form-group col-lg-4">
							{{ form_label(edit_form.localidad) }}
							{{ form_widget(edit_form.localidad) }}
					</div>
						
					<div class="form-group col-lg-4">
							{{ form_label(edit_form.users) }}
							{{ form_widget(edit_form.users) }}
					</div>	
	
				{{ form_widget(edit_form) }}
			
					</div>
			
			    <div class="row">
					<div class="col-lg-12">
			
					<input type="submit" class="btn btn-primary" value="Editar" />					
					{{ form_end(edit_form) }}
					
					{{ form_start(delete_form) }}
						<input type="submit" class="btn btn-danger" value="Eliminar" />					
					{{ form_end(delete_form) }}

					
				</div>  
			</div>

	
			
					</section>	
			</div>
		</div>
{% endblock %}