{% extends 'layout.html.twig' %}
{% block body %}
	<!--overview start-->
    <div class="row">
        <div class="col-lg-12">
            <h3 class="page-header"><i class="icon_house_alt" aria-hidden="true"></i> Lista de Usuarios</h3>
            <a class="btn btn-info" href="{{ path('fos_user_registration_register') }}" title="Nueva unidad de carga"><i class="fa fa-plus-square" aria-hidden="true"></i>  Nuevo Usuario </a>      
        </div>      
    </div>
    <br>
	<br>
	<div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                      
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="dataTable_wrapper">
                        <table class="table table-striped table-bordered table-hover" id="dataTables-example">
							<thead>
								<tr>
									<th>Nombre de Usuario</th>
									<th>Email</th>		
                                <!--     {% if is_granted('ROLE_ADMIN') %}								
									  <th>Acciones</th>
                                    {% endif %}
								</tr>
							</thead> -->
							<tbody>
								{% for usuario in Usuarios %}
								<tr class="odd gradeX">
									<td>{{ usuario.username }}</td>
									<td>{{ usuario.email }}</td><!-- 
                                    {% if is_granted('ROLE_ADMIN') %}   
               						<td>
                   					<ul>
                        			<li>
                           				<a class="btn btn-info" href="{{ path('fos_user_change_password', { 'id': usuario.id }) }}" title="Ver unidad de carga"> Modificar Contraseña(FALTA HACER)</a>
                        			</li>
                    				</ul>
                					</td>  
                                    {% endif %}-->
		 						</tr>	
        						{% endfor %}
        					</tbody>
    					</table>
                    </div>
                            <!-- /.table-responsive -->
                </div>
                        <!-- /.panel-body -->
            </div>
                    <!-- /.panel -->
        </div>
                
    </div><!--/.row-->
{% endblock %}
